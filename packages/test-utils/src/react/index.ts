/**
 * React testing utilities for component testing with providers.
 *
 * Provides helper functions for rendering components with ThemeProvider and other context providers.
 */

export {
  createTestWrapper,
  renderWithProviders,
  renderWithTheme,
  type RenderWithProvidersOptions,
  type RenderWithThemeOptions,
} from './render-utils'
