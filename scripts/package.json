{
  "name": "scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Sparkle scripts.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@sparkle/test-utils": "workspace:*",
    "vitest": "^4.0.0"
  }
}
