---
"@sparkle/theme": patch
---

Comparing a theme value against `null` now reports a difference instead of throwing. Merging an object over a non-object value now replaces it cleanly instead of merging by character index.
