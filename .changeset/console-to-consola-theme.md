---
"@sparkle/theme": patch
---

Replace `console.warn`/`console.error` calls with `consola` in `persistence`, `ThemeProvider`, and `NativeThemeProvider` to follow the project's structured logging convention.
